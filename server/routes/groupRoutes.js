var express = require('express');
var router = express.Router();
var ObjectId = require('mongoose').Types.ObjectId;

var Group = require('../models/group');

router.post('/groupTest', function (req, res, next) {
});

module.exports = router;